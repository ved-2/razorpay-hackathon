{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @commerceos/database build && tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@commerceos/database": "workspace:*"
  }
}